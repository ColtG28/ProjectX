`stage_100` targets approximately:

- 100 benign fixtures
- 100 suspicious-safe fixtures

The harness materializes these cases at runtime from stored seeds plus deterministic generated binary-adjacent fixtures.
